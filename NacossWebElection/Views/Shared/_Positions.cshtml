
@{
    ViewBag.Title = "_Position";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@model IEnumerable<NacossWebElection.Models.DBModel.Position>

@*<form class="form-default">

    <div class="form-group">
        <input type="text" class="form-control" id="exampleInputEmail" placeholder="Search voters">
        <span></span>
    </div>
    <button type="submit" class="btn btn-default">Search for voters <i class="fa fa-search"></i></button>
</form>*@
<h2> List of Positions</h2>
<div class="scroll-table">
    <span id="tblError" class="text-danger"></span>
    <table class="table table-responsive table-bordered" id="tbl_candidate">
        <thead>
            <tr>
                <th>S/n</th>
                <th>Position ID</th>
                <th>Position</th>
            </tr>


        </thead>




        <tbody>
            @{int a = 0;}
            @foreach (var i in Model)
            {

                string formatedID = i.PositionID.ToString();
                if (Model != null)
                {
                    <tr id="row_@formatedID">
                        @{a++;}
                        <td>
                            @a
                        </td>
                        <td>@i.PositionID</td>
                        <td>@i.Position1</td>
                        
                        <td><a href="@Url.Action("EditPosition","Admin",new { id= i.PositionID})" class="btn btn-xs btn-primary">Edit</a> | <a href="@Url.Action("DeletePosition","Admin", new { id = i.PositionID })" data-formated-id="@formatedID" class="btn btn-xs btn-default delete">Delete <img src="~/Images/ajax/LoadingCircle.gif" class="hide-loader" id="img_@formatedID" width="20" height="20" /></a></td>
                    </tr>
                            }
                            else
                            {
                                <tr>

                                    <td colspan="6">
                                        <h4>No Positon found</h4>
                                    </td>

                                </tr>
                                }

                            }

            <tr id="load">
                <td colspan="5">
                    <div class="loader-cirle"></div>
                    <h3>Loading...</h3>
                    <script>
                        var cn;
                        @if(a >= 0)
                        {
                            @: cn = @a+1;
                        }
                        else
                        {
                             @: cn = 1;
                        }
                    </script>
                </td>
            </tr>

        </tbody>
        <tfoot>
            <tr>
                <td colspan="6">
                    <nav aria-label="Page navigation">
                        <ul class="pagination">
                            <li>
                                <a href="#" aria-label="Previous">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                            <li><a href="#">1</a></li>
                            <li><a href="#">2</a></li>
                            <li><a href="#">3</a></li>
                            <li><a href="#">4</a></li>
                            <li><a href="#">5</a></li>
                            <li>
                                <a href="#" aria-label="Next">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                        </ul>
                    </nav>
              
                </td>
            </tr>
        </tfoot>
    </table>
</div>


